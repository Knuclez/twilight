En el main, hay que cambiar la funcion de render que tiene un switch para cada escena

En el g_ents, hay q cambiar la funcion load_scene_gui_entities que es un switch para cargar las entidades
 de cada screen especifica, y en caso de ser necesario la funcion de los flags.\

En el input, las dunciones d e todos los botones tienen un switch interno que depende de la escena y habria
 que cambiarlo

